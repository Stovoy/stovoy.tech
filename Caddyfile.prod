# Production Caddyfile for stovoy.tech – automatic HTTPS via Let's Encrypt

stovoy.tech, www.stovoy.tech {
    encode zstd gzip

    # Redirect legacy healthz route
    @api path /api/* /healthz
    reverse_proxy @api backend:8080

    # Serve static files built by Trunk (copied during container build)
    root * /site
    file_server
}
